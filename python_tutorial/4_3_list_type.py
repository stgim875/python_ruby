# <데이터 타입>
# List Type(2021-11-02)

import statistics
import random

name = ["gst", "kst", "gim", "kim"]
grades = [2, 1, 4]
print("name[1]", name[1])
print("len(name)", len(name))
print("min(grades)", min(grades))
print("max(grades)", max(grades))
print("max(grades)", sum(grades))

print("statistics.mean(grades)", statistics.mean(grades))
print("(random.choice(name))", (random.choice(name)))
